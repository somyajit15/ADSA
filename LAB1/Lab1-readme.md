Binary vs Ternary Search: Program compares the performance and number of comparisons required in binary search and ternary search on sorted data.

Compare Complexity: Program analyzes and compares the time complexities of different algorithms to demonstrate how input size affects execution time.

Defective Coin Problem: Program identifies the single defective coin among a set using minimum number of weighings based on divide-and-conquer strategy.

Heapsort: Program implements heap sort by constructing a max-heap and repeatedly extracting the maximum element to sort the array.
